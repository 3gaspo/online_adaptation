Updated: 2026-08-28

Scope: this handoff records only Slurm artifacts published by current commit
34d3417. Jobs and outputs deleted before that commit are not retained as
history here.

Synchronized job:
- s_online_main, job 2913487: 01_main_online_ridge, test profile, Selena.
  The extraction processed all 9,090 compact windows, then failed with disk
  quota exceeded while creating the final table directory. The same quota
  failure prevented the exception handler from writing manifest.json.tmp, so
  the sole online_extraction manifest remains marked running without a
  ready_at_utc timestamp.

Current artifact state: one running extraction manifest and zero completed
scientific runs. No main adaptation, table, gate, or report completed.

Next actions:
1. Put the one-line NNI file at $HOME/codes/.secrets/nni on DGX and Selena.
2. Move the current Selena payload to
   /scratch/users/<lowercase-nni>/codes/online_adaptation/outputs_selena and
   logs_selena, then run sync_code_to_selena.sh to deploy the scratch-aware
   fronts and ensure both scratch directories exist.
3. Reconcile terminal launch 2913487 with the supported interrupt-launch
   lifecycle command; because the manifest is not ready, it should become
   interrupted rather than completed. Do not edit the manifest directly.
4. Rerun slurm/selena/main/01_main_online_ridge_selena.slurm in test mode.
5. After the main test completes, run
   slurm/selena/main/02_online_gates_selena.slurm in test mode.

Runtime roots: DGX uses project-local outputs/ and logs/. Selena uses the
project-specific outputs_selena/ and logs_selena/ trees under
/scratch/users/<lowercase-nni>/codes/online_adaptation/. Result
synchronization remains a DGX-initiated inbound pull.
