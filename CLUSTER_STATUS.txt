Updated: 2026-08-26
Latest completed workflow: none (new project)
Artifacts analyzed: none
Current/next workflow: from the project root, submit slurm/dgx/main/01_main_online_ridge.slurm with EXPERIMENT_MODE=test; after its DGX contract is checked, submit slurm/selena/main/01_main_online_ridge_selena.slurm with EXPERIMENT_MODE=test to validate Selena scheduling, isolated outputs_selena/logs_selena, and result synchronization
Known job IDs/dependencies: none
Notes: all 16 DGX fronts under slurm/dgx/ now have scientific-equivalent Selena counterparts under slurm/selena/. First validate the normalized v1 window/query/context tables, cadence alignment, and all-user batch timing with the main ridge front in test mode; then validate the Selena transfer boundary, the source-faithful TS-RAG path, and the online-gates front in test mode before scaling. Selena uses QoS an_preemptable. No Selena front or transfer has yet run.
Runtime roots are always overridable: DGX defaults to outputs/ and logs/, while Selena explicitly selects outputs_selena/ and logs_selena/.
Result synchronization runs on DGX as an inbound Selena pull. An unscoped publisher run includes the paired Selena artifact trees under the standard lightweight exclusions.
