Updated: 2026-08-28

CURRENT SCHEDULER STATE
- The user reported submitting all four deadline Selena jobs on 2026-08-28.
  Job IDs and current scheduler states were not supplied.
- The fixed-protocol remainder was submitted with `afterok` dependencies on
  the fully online per-user and fully fixed shared anchors.
- The earlier unverified main/gate submission entries are no longer treated as
  active because no job IDs or terminal outcomes were supplied.
- No completed current-protocol scientific result is synchronized locally.

RETAINED DIAGNOSTIC HISTORY
- s_online_main, job 2913487, test profile on Selena, failed under the obsolete
  home-quota routing while writing final artifacts. Its result payload is not
  reusable; retained logs are diagnostic evidence only.

SUBMITTED DEADLINE JOBS
The following Selena jobs were user-reported as submitted. Their job IDs and
scheduler states remain unknown until the submission output or an `squeue`
snapshot is supplied.

1. Fully online per-user fixed-protocol anchor:
   `sbatch slurm/selena/deadline/fixed_online_per_user_selena.slurm`
2. Fully fixed shared fixed-protocol anchor:
   `sbatch slurm/selena/deadline/fixed_fixed_shared_selena.slurm`
3. Remaining fixed-protocol arms, after both anchors succeed:
   `sbatch slurm/selena/deadline/fixed_ablation_remainder_selena.slurm`
4. Priority ridge-versus-TS-RAG comparison:
   `sbatch slurm/selena/deadline/tsrag_priority_t3_selena.slurm`

Next, record each job ID and current scheduler state. After termination, pull
the corresponding logs and lightweight report artifacts to DGX, publish them,
and replace the user-reported state with verified terminal outcomes before
analysis.

RUNTIME ROOTS
- DGX: project-local outputs/ and logs/.
- Selena: /scratch/users/<lowercase-nni>/codes/online_adaptation/outputs_selena
  and logs_selena.
- Result synchronization is a DGX-initiated inbound pull. Lightweight sync is
  the default; detailed and full are explicit diagnostic/recovery tiers.
