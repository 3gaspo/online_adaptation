Updated: 2026-08-29

CURRENT SCHEDULER STATE
- Replacement online-per-user anchor 2964505 and fixed-shared anchor 2964506
  both reached their final Solar adaptation in synchronized stdout and have
  empty stderr. Their logs predate terminal workflow markers, so their final
  scheduler states are not independently verified locally.
- Fixed-protocol remainder 2964508 completed all 12 requested adaptations and
  then failed in the table stage because extraction-specific hashes split
  equivalent vanilla rows into different model labels. Its synchronized
  24-row detailed result is valid input, but the comparison table is not.
- Priority TS-RAG replacement 2964507 completed all 16 extractions and the
  first Electricity ridge adaptation, then failed when its source-adapted ARM
  imported a forecasting class from the thin official Chronos-Bolt adapter.
- s_dl_fixed, job 2964440, failed during the first adaptation task after both
  fixed extractions completed. Virtual-array indexing truncated context time
  axes and Chronos rejected a context shorter than 168 values.
- s_dl_tsrag, job 2964441, failed on task 2/16 because the Chronos-T5 tokenizer
  received CUDA input while its bucket boundaries were on CPU.
- s_dl_online, job 2964439, was still running when its synchronized log ended
  at 12,800/15,462 Electricity extraction windows. Its old configuration is
  invalid and should be cancelled rather than published.
- s_online_main, job 2955394, failed immediately because the obsolete
  quota-failed run still had a `running` manifest.
- No current-protocol final comparison has completed.

RETAINED DIAGNOSTIC HISTORY
- s_online_main, job 2913487, test profile on Selena, failed under the obsolete
  home-quota routing while writing final artifacts. Its result payload is not
  reusable; retained logs are diagnostic evidence only.

NEXT EXECUTION
- Deploy the current code, which isolates the pinned TS-RAG Chronos-Bolt core,
  collapses equivalent vanilla sources to one readable backbone label, and
  emits terminal task, stage, and workflow records.
- Resume TS-RAG with `STAGES=adapt,tables`; completed extractions and the first
  ridge adaptation remain reusable under the current contract.
- Regenerate the fixed comparison with `STAGES=tables`; no fixed-protocol
  extraction or adaptation rerun is required.

RUNTIME ROOTS
- DGX: project-local outputs/ and logs/.
- Selena: /scratch/users/<lowercase-nni>/codes/online_adaptation/outputs_selena
  and logs_selena.
- Result synchronization is a DGX-initiated inbound pull. Lightweight sync is
  the default; detailed and full are explicit diagnostic/recovery tiers.
